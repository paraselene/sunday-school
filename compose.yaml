services:
  web:
    build: .
    env_file: .env
    environment:
      DATABASE_PATH: /data/db.sqlite3
      DEBUG: "0"
    volumes:
      - ./data:/data
      - ./fonts:/app/fonts:ro
    restart: unless-stopped
    networks:
      - default
      - proxy

networks:
  proxy:
    name: wordpress_wordpress_net
    external: true
