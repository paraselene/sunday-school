services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_PATH: /data/db.sqlite3
      DEBUG: "0"
    volumes:
      - sunday_school_data:/data
    restart: unless-stopped

volumes:
  sunday_school_data:
